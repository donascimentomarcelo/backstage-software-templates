apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{values.name}}
  description: Python app that displays time - named ${{values.name}}.
  annotations:
    github.com/project-slug: ricardoandre97/${{values.name}}
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  owner: development
  lifecycle: experimental